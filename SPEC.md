# ゲーム仕様（シミュレータが実装しているルール）

出典: 動画解析プロジェクト bomberman-analysis（スーパーボンバーマンR2「ギンギンパワー」1対1、配信映像を60コマ/秒で計測）の
`analysis/escape.py`・`events.py`・`link_throws.py`・`RECIPES.md` と、その実測値。
**確認済み** = 映像の計測で裏付けがある値。**仮定** = 未確認で、シミュレータ用に置いた値（`bomberman_ai/constants.py` に同じ印を付けてある）。

## 盤面
- 確認済み: 13列×11行。柱は0始まりで「奇数列×奇数行」（6×5=30本）。壊せるブロックは無い（このモードは開けた盤面）。
- 確認済み: 座標は列 c（左→右）、行 r（上→下）。プレイヤーの初期位置は対角の角（仮定: (0,0) と (12,10)。映像では READY 中に動ける）。

## 時間
- 確認済み: 60コマ/秒。制限時間 2:00（時間切れは引き分け扱い。仮定）。
- 未実装: ステージの縮小（ユーザー設定で時刻が変わる）。READY/GO の演出（開始から約1.7秒後に GO、その間も移動できる）。

## 移動
- 確認済み: 最大スピードで 1マス 6コマ（0.1秒）。
- 仮定（単純化）: マスの中心から隣の中心へ 6コマで移動し、途中の方向転換は「反転」だけ可。プレイヤー同士はすり抜ける。
- 確認済み: 柱・盤外には入れない。地面の爆弾のマスには入れない（入ろうとするとキック）。自分が乗っている爆弾のマスからは出られる。
- 当たり判定に使うマス: 移動が半分（3コマ）を越えたら移動先のマス（仮定）。

## 爆弾
- 確認済み: 設置から 150コマ（2.5秒）で爆発（実測の中央値148）。同じマスに2個は置けない。
- 仮定: 同時に置ける数 8（ギンギンパワー＝最大）。
- 確認済み: 爆風は上下左右に最大 8マス。柱で止まる。別の爆弾に届くとそこで止まり、その爆弾は 10コマ後に爆発（誘爆）。
- 確認済み: 爆炎は 30コマ残る。
- 確認済み: 抱えている間は爆発しない。投げて着地した時点からカウントが始まる（着地後の寿命の中央値 153コマ）。

## モーションの振りかぶり（windup）
- 確認済み（ユーザー確認、2026-09-21）: パンチ・キック・投げには、それぞれ専用のモーションがあり、入力してから実際に効果（爆弾が飛ぶ・滑り出す・手を離れる）が
  起きるまでに若干の硬直（振りかぶり）がある。3つとも入力した瞬間に即座には発動しない。
- 確認済み（ユーザー確認）: キックは、足元（自分がいまいるマス）に置いた爆弾をその場では蹴れない。蹴るには必ず隣のマスから、その爆弾へ向かって
  移動する（＝キックの入力そのものは既存の「隣接マスへの移動」で表現される）必要があり、いったん爆弾から離れてから改めて蹴り込む必要がある場合、
  その分の移動時間もかかる。さらに、隣接マスから蹴り込む動作そのもの（蹴り足を出す瞬間まで）にも時間がかかる。
- 仮定: 振りかぶりの長さは PUNCH_WINDUP・THROW_WINDUP・KICK_WINDUP いずれも 6コマ（0.1秒）。実測値ではなく、
  上記の「硬直がある」という事実（ユーザー確認）に対して置いた仮の値。GPT・今後の映像計測で精緻化すること。
- 実装: 振りかぶり中は対象の爆弾を `Player.queued`（kind/at/bomb_id/dir）として予約し、`at` コマに達したら発動する
  （`engine._resolve_queued`、処理順序の 5.5）。振りかぶり中に対象の爆弾が消えていれば（誘爆等）不発になる。

## キック
- 確認済み: 隣のマスの爆弾へ向かって移動しようとするとキックの振りかぶりが始まり（自分は移動しない）、KICK_WINDUP コマ後に
  爆弾が一直線に滑り出す。柱・盤の端・別の爆弾・人で止まる。
- 仮定: 滑り出してからは 1マス 5コマ（KICK_STEP）で滑る（実測は1コマ0.1マス超）。滑っている間も爆発時刻は進む（キックで時刻は変わらない）。

## パンチ
- 確認済み: 向いている隣のマスの爆弾へパンチの振りかぶりを始め（PUNCH_WINDUP コマ）、その後に前方へ飛ばす。距離の中央値 3マス（実測 2〜5）。
- 仮定: 着地先が柱・爆弾なら同じ方向へ1マスずつ先へ。盤外へは出ず端で止まる。飛行 20コマ。爆発時刻は変わらない。
- 確認済み（現象）: 着地したマスにいる人は「ぴよって」動けなくなる。仮定: 60コマ。
- 仮定: 振りかぶり（PUNCH_WINDUP）に加え、発動後さらに 10コマ移動できない（硬直、ACTION_LAG）。

## 抱えて投げる
- 確認済み: 乗っている爆弾を拾って抱え、投げの振りかぶり（THROW_WINDUP コマ）の後に前方へ投げる。着地からカウント再開。距離の中央値 6マス（実測 3〜9）。
- 仮定: 拾う動作 6コマ、投げの振りかぶりに加え発動後さらに 10コマ硬直、飛行 20コマ、着地先の処理はパンチと同じ、着地マスの人は気絶。

## 死亡・勝敗
- 確認済み: 爆炎のマスに触れた瞬間に死亡（無敵時間なし。実測: 逃げ遅れの判定は「燃え始めの0.5秒前までに通り抜けられたか」で行っている）。
- 仮定: 同じコマで両者が死んだら引き分け。片方が死んだら相手の勝ち。制限時間で引き分け。
- 解析側の負けの原因分類: 自滅（自分の爆弾）、キック（相手のキックで届いた爆弾）、パンチ被弾（頭に当たって気絶後）、設置。シミュレータは死因を own_bomb / opp_bomb で記録する。

## 1コマ内の処理順序（両者が同時に行動する場合）
`engine.step(state, a0, a1)` は次の順に処理する（仮定。動画からは順序を確定できない）:
1. コマ番号を +1（t）
2. 消える時刻が来た爆炎を消す
3. 飛んでいる爆弾が着地する（着地マスの人は気絶。投げた物は着地からカウント）
4. 滑っている爆弾が進む（次のマスが塞がっていれば止まる）
5. 爆発時刻が来た爆弾が爆発する（複数なら同じコマに全部。誘爆は10コマ後）
5.5. 振りかぶり中だったパンチ・投げ・キックのうち、windup が終わった物が実際に発動する（対象の爆弾が既に無ければ不発）
6. 操作（設置・パンチ・拾う・投げる）をプレイヤー0、1の順に適用。PUNCH/THROW/KICK はここでは即座に発動せず、振りかぶりの予約だけを行う（5.5 で発動）。同じマスへの同時設置は0が優先
7. 移動を両者同時に適用（移動先に地面の爆弾があれば、その場でキックの振りかぶりを予約する＝移動はしない）
8. 爆炎のマスにいる人は死亡。両方なら引き分け。時間切れなら引き分け

## 解析側の「逃げ込めるマス」との対応
`bomberman_ai/safety.py` は解析側 `escape.py` と同じ考え方（燃え始め時刻 L、誘爆の連結、最初の爆発までの持ち時間、1マス6コマ、余裕 margin）。
解析側は実戦の誤差のため margin=30コマ（0.5秒）を使う。シミュレータ内は時刻が正確なので既定 margin=0。

## Safety Solver の適用範囲（GPT 改修）

上流 `c2380f7` のルール定数・処理順序・Player.queuedを維持した。`engine.advance_environment`
は処理2〜5.5を共通関数に切り出したもの。予測にも予約動作を含める。以下の予測方式を区別する。

| 関数 | 扱う内容 | 保証の範囲・制約 |
|---|---|---|
| `temporal.forecast` | 誘爆10コマ、爆炎30コマ、飛行・着地、滑走、爆弾消滅後の爆風経路 | プレイヤーは現在位置に固定。滑走を止める人の位置が変われば予測も変わる |
| `safety.escape_area` | 最初の爆発までに到達する永久避難先、最初の出発方向別の逃走路 | 保守的な特徴量。0でも待機・炎消滅・爆弾操作で助かる場合がある。解析側と数値は完全一致しない |
| `temporal.survival` | 1コマ待機、6コマ移動、移動中の半マス判定、現在の気絶・硬直、炎消滅後の再進入 | 予測済み爆弾配置下で移動のみ。将来のキック・パンチ等は探索しない。着地コマへの滞在は安全側に除外。空集合を「確定死」と呼ばない |
| `safety.bounded_minimax` | 各コマで自分の行動→相手の全合法応手、実エンジンで同時実行 | 指定した有限コマまでのみ。`safe/unsafe/unknown`を返す。探索予算切れはunknown。実戦の緊急判定では2コマ・128ノード |
| `offense.robustness` | 相手の設置・キック・パンチ・拾う・投げを含む1回の応手 | 操作は最初の1コマのみ。その後は移動継続。安全な応手の比率であり勝率・安全保証ではない |

長期の全行動min-maxを爆発時刻まで完全に解くものではない。保守的な近似と、有限時間の
厳密探索を混同しない。`escape_routes`は「生存につながる最初の出発方向数」であり、
頂点を共有しない独立経路の最大本数ではない。

## 実測値への置き換え状況

このリポジトリには `all_moves.csv` / `all_bombs.csv` / 元映像がなく、確認できた共有先にも
解析プロジェクトは見つからなかった。次の値は**変更せず仮定を維持**する。

| 定数 | 現在値 | 更新に必要な証拠 |
|---|---:|---|
| KICK_STEP | 5コマ/マス | 蹴り開始・停止・移動距離の複数区間 |
| FLY_FRAMES | 20コマ | 発射から着地までのフレーム差 |
| STUN | 60コマ | 被弾から操作再開までの差 |
| ACTION_LAG | 10コマ | 操作開始から移動再開までの差 |
| MAX_BOMBS | 8 | 同時設置数の上限に達した複数場面 |
| THROW_DIST | 6マス | 障害物・盤外処理を区別した投げの距離 |
| PICKUP_FRAMES | 6コマ | 拾う開始から次の操作可能時刻 |

実測へ更新する際は、匿名化した入力、サンプル数、推定方法、外れ値処理、検証用の別場面を
記録し、対応するルールテストも更新する。数値を推測して実測済みとはしない。

発動前硬直の PUNCH_WINDUP / THROW_WINDUP / KICK_WINDUP は上流で各6コマと仮定された。
この統合ではその値を維持した。実測済みではなく、個別の計測が必要。
