# ゲーム仕様（シミュレータが実装しているルール）

出典: 動画解析プロジェクト bomberman-analysis（スーパーボンバーマンR2「ギンギンパワー」1対1、配信映像を60コマ/秒で計測）の
`analysis/escape.py`・`events.py`・`link_throws.py`・`RECIPES.md` と、その実測値。
**確認済み** = 映像の計測で裏付けがある値。**仮定** = 未確認で、シミュレータ用に置いた値（`bomberman_ai/constants.py` に同じ印を付けてある）。

## 盤面
- 確認済み: 13列×11行。柱は0始まりで「奇数列×奇数行」（6×5=30本）。壊せるブロックは無い（このモードは開けた盤面）。
- 確認済み: 座標は列 c（左→右）、行 r（上→下）。プレイヤーの初期位置は対角の角（仮定: (0,0) と (12,10)。映像では READY 中に動ける）。

## 時間
- 確認済み: 60コマ/秒。制限時間 2:00（時間切れは引き分け扱い。仮定）。
- 未実装: ステージの縮小（ユーザー設定で時刻が変わる）。READY/GO の演出（開始から約1.7秒後に GO、その間も移動できる）。

## 移動
- 確認済み: 最大スピードで 1マス 6コマ（0.1秒）。
- 仮定（単純化）: マスの中心から隣の中心へ 6コマで移動し、途中の方向転換は「反転」だけ可。プレイヤー同士はすり抜ける。
- 確認済み: 柱・盤外には入れない。地面の爆弾のマスには入れない（入ろうとするとキック）。自分が乗っている爆弾のマスからは出られる。
- 当たり判定に使うマス: 移動が半分（3コマ）を越えたら移動先のマス（仮定）。

## 爆弾
- 確認済み: 設置から 150コマ（2.5秒）で爆発（実測の中央値148）。同じマスに2個は置けない。
- 仮定: 同時に置ける数 8（ギンギンパワー＝最大）。
- 確認済み: 爆風は上下左右に最大 8マス。柱で止まる。別の爆弾に届くとそこで止まり、その爆弾は 10コマ後に爆発（誘爆）。
- 確認済み: 爆炎は 30コマ残る。
- 確認済み: 抱えている間は爆発しない。投げて着地した時点からカウントが始まる（着地後の寿命の中央値 153コマ）。

## キック
- 確認済み: 隣のマスの爆弾へ向かって移動するとキックになり、爆弾は一直線に滑る。柱・盤の端・別の爆弾・人で止まる。
- 仮定: 1マス 5コマで滑る（実測は1コマ0.1マス超）。滑っている間も爆発時刻は進む（キックで時刻は変わらない）。

## パンチ
- 確認済み: 向いている隣のマスの爆弾を前方へ飛ばす。距離の中央値 3マス（実測 2〜5）。
- 仮定: 着地先が柱・爆弾なら同じ方向へ1マスずつ先へ。盤外へは出ず端で止まる。飛行 20コマ。爆発時刻は変わらない。
- 確認済み（現象）: 着地したマスにいる人は「ぴよって」動けなくなる。仮定: 60コマ。
- 仮定: パンチ後は 10コマ移動できない（硬直）。

## 抱えて投げる
- 確認済み: 乗っている爆弾を拾って抱え、前方へ投げる。着地からカウント再開。距離の中央値 6マス（実測 3〜9）。
- 仮定: 拾う動作 6コマ、投げた後 10コマ硬直、飛行 20コマ、着地先の処理はパンチと同じ、着地マスの人は気絶。

## 死亡・勝敗
- 確認済み: 爆炎のマスに触れた瞬間に死亡（無敵時間なし。実測: 逃げ遅れの判定は「燃え始めの0.5秒前までに通り抜けられたか」で行っている）。
- 仮定: 同じコマで両者が死んだら引き分け。片方が死んだら相手の勝ち。制限時間で引き分け。
- 解析側の負けの原因分類: 自滅（自分の爆弾）、キック（相手のキックで届いた爆弾）、パンチ被弾（頭に当たって気絶後）、設置。シミュレータは死因を own_bomb / opp_bomb で記録する。

## 1コマ内の処理順序（両者が同時に行動する場合）
`engine.step(state, a0, a1)` は次の順に処理する（仮定。動画からは順序を確定できない）:
1. コマ番号を +1（t）
2. 消える時刻が来た爆炎を消す
3. 飛んでいる爆弾が着地する（着地マスの人は気絶。投げた物は着地からカウント）
4. 滑っている爆弾が進む（次のマスが塞がっていれば止まる）
5. 爆発時刻が来た爆弾が爆発する（複数なら同じコマに全部。誘爆は10コマ後）
6. 操作（設置・パンチ・拾う・投げる）をプレイヤー0、1の順に適用。同じマスへの同時設置は0が優先
7. 移動を両者同時に適用（移動先の爆弾はキック。両者が同じ爆弾を反対側から蹴ると、先に処理される0の向きになる）
8. 爆炎のマスにいる人は死亡。両方なら引き分け。時間切れなら引き分け

## 解析側の「逃げ込めるマス」との対応
`bomberman_ai/safety.py` は解析側 `escape.py` と同じ考え方（燃え始め時刻 L、誘爆の連結、最初の爆発までの持ち時間、1マス6コマ、余裕 margin）。
解析側は実戦の誤差のため margin=30コマ（0.5秒）を使う。シミュレータ内は時刻が正確なので既定 margin=0。
